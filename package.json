{
  "name": "flutter-web-app",
  "version": "1.0.0",
  "scripts": {
    "predeploy": "flutter build web",
    "deploy": "gh-pages -d build/web"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  }
}
